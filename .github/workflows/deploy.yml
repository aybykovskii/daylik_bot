name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy via Ansible
    runs-on: ubuntu-latest

    steps:
      - name: Run Workflow
        uses: ./.github/actions/run-workflow
        with:
          workflow: 'ansible:deploy'
